@model FormContatoViewModel

@{
    ViewBag.index = ViewBag.index != null ? ViewBag.index : 0;
}
<div class="form-group">
    <label>Contatos</label>
    <button type="button" id="btnAddContato" class="btn btn-xs btn-info"><i class="fa fa-plus"></i></button>
</div>
<div class="form-group formContato">
    <div class="input-group">
        <span class="input-group-btn">
            <button type="button" class="btnDelContato btn btn-danger"><i class="fa fa-close"></i></button>
        </span>

        @foreach (var item in Model.TiposContato)
        {
            <span class="input-group-addon">
                <input type="radio" name="Contatos[@ViewBag.index].TipoContato.id" value="@item.id" /> @item.descricao
            </span>
        }

        <input type="text" class="form-control" name="Contatos[@ViewBag.index].Telefone" />
    </div><!-- /input-group -->
    @*<div class="form-group col-md-5">
            <label asp-for="contato.TipoContato"></label>
            <select class="form-control" name="contato[@ViewBag.index].TipoContato" asp-items="@(new SelectList(Model.TiposContato,"id","descricao"))"></select>
        </div>
        <div class="form-group col-md-6">
            <label asp-for="contato.Telefone"></label>
            <input class="form-control" name="contato[@ViewBag.index].Telefone" />
        </div>
        <div class="form-group col-md-1">
            <button type="button" class="btnDelContato btn btn-xs btn-danger"><i class="fa fa-close"></i></button>
        </div>*@
</div>